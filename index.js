// Write your code in this file!
 // Defining who the current user is
const currentUser = 'Lukas';
const string ='Welcome to Flatbook,';



// Setting a variable to our welcome message

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Setting a variable to have an excited welcome message!
const upperCaseString = string.toUpperCase();
const upperCaseCurrentUser = currentUser.toUpperCase();
const excitedWelcomeMessage = `${upperCaseString} ${upperCaseCurrentUser}!`;

// Setting a variable to have a shortGreeting

const shortGreeting = `Welcome, ${currentUser[0]}!`;
